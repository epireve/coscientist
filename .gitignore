# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Envs
.venv/
venv/
env/
.env
.env.local

# uv
.uv/
# uv.lock IS committed — coscientist is an app, not a lib.
# CI uses it for reproducible installs + caching.

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Coscientist runtime state
.coscientist/
cache/
node_modules/

# Institutional-access secrets — never commit these
.claude/skills/institutional-access/state/
.claude/skills/institutional-access/scripts/**/storage_state.json
.claude/skills/institutional-access/scripts/**/cookies.json

# Claude local settings (user-specific)
.claude/settings.local.json

# Test harness scratch files
tests/_tmp_input.json
tests/_tmp_phase_*.json
tests/_tmp_dogfood_input.json

# Root .mcp.json holds API keys — kept local; .mcp.json.example is tracked.
# Plugin .mcp.json files are checked in (no secrets — placeholder paths).
/.mcp.json

# Local cache + locks (per-machine state)
.arxiv2md_cache/
.claude/scheduled_tasks.lock

# Persistent Chrome profile for institutional-access (cookies, history)
.claude/skills/institutional-access/state/chrome_profile/

# Plugin build artifacts (timestamped + variant builds)
coscientist-cowork.zip
coscientist-cowork-*.zip
coscientist-*.plugin
coscientist.plugin
validate_plugin.py

# Scratch / one-off research notes (not for repo)
cancer-research-phase0.md
ziXH4nRf
